{% extends 'base.html.twig' %}

{% block title %}Auto{% endblock %}

{% block body %}
    <h1>Auto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auto.id }}</td>
            </tr>
            <tr>
                <th>Marca</th>
                <td>{{ auto.marca }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ auto.color }}</td>
            </tr>
            <tr>
                <th>Capacidad</th>
                <td>{{ auto.capacidad }}</td>
            </tr>
            <tr>
                <th>Modelo</th>
                <td>{{ auto.modelo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_auto_index') }}">back to list</a>

    <a href="{{ path('app_auto_edit', {'id': auto.id}) }}">edit</a>

    {{ include('auto/_delete_form.html.twig') }}
{% endblock %}
